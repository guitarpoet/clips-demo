[
        {
                "field": "id",
				"state": "readonly",
                "label": "ID",
                "rules": [
					{"type":"number"},
					"required"
				],
				"messages": {
					"required": "The id field must be set"
				}
        },
        {
                "field": "status",
                "label": "Status",
                "rules": [{
					"type": "number",
					"minlength": 3,
					"maxlength": 10
				}, "required"],
				"defaultValue":"hello",
				"messages": {
					"required": "The status field must be set"
				}
        },
        {
                "field": "create_date",
                "label": "Create Date",
				"state": "readonly"
        },
        {
                "field": "date",
                "label": "Date",
                "rules": [
                    {"type":"text"},
                    {"data-role":"date"},
                    "required"
                ],
                "messages": {
                    "required": "The id field must be set"
                }
        },
        {
            "field": "year",
            "label": "Year",
            "rules": [
                {"type":"text"},
                {"data-role":"year"},
                "required"
            ],
            "messages": {
                "required": "The id field must be set"
            }
        },
        {
            "field": "month",
            "label": "Month",
            "rules": [
                {"type":"text"},
                {"data-role":"month"},
                "required"
            ],
            "messages": {
                "required": "The id field must be set"
            }
        }
]
