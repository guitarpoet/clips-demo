[
        {
                "field": "id",
				"state": "readonly",
                "label": "ID",
                "rules": [
					{"type":"number"},
					"required"
				],
				"messages": {
					"required": "The id field must be set"
				}
        },
        {
                "field": "status",
                "label": "Status",
                "rules": [{
					"type": "number",
					"minlength": 3,
					"maxlength": 10
				}, "required"],
				"defaultValue":"hello",
				"messages": {
					"required": "The status field must be set"
				}
        },
        {
                "field": "create_date",
                "label": "Create Date",
				"state": "readonly"
        }
]
