[
        {
                "field": "id",
				"state": "readonly",
                "label": "ID",
                "rules": [
					{"type":"number"},
					"required"
				],
				"messages": {
					"required": "This field must be set"
				}
        },
        {
                "field": "status",
                "label": "Status",
				"defaultValue":"hello",
                "rules": [
					{"type":"number"},
					"required"
				],
				"messages": {
					"required": "This field must be set"
				}
        },
        {
                "field": "create_date",
                "label": "Create Date",
				"state": "readonly"
        }
]
